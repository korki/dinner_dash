<h3><%= @category.name %> </h3>

<% if !@category.items.nil? %> 
  <ul id="category_items">
    <% @category.items.each do |item| %>
    <li>
     <%= item.name %>
    </li>
    <% end %> 
  </ul>
<% end %>